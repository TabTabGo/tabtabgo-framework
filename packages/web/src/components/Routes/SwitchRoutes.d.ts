export default SwitchRoutes;
declare function SwitchRoutes(routes: any): JSX.Element;
declare namespace SwitchRoutes {
    namespace propTypes {
        const routes: PropTypes.Validator<object[]>;
    }
}
import PropTypes from "prop-types";
