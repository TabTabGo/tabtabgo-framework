const editorStyles = (theme) => ({
  root: {
    '& img': {
      maxWidth: '100%',
    },
    '& h1': {
      fontSize: '2em',
    },
    '& h2': {
      fontSize: '1.8em',
    },
  },
});

export default editorStyles;
